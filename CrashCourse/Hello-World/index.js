var age = 20; // now we have set age to a number we cannot covert it to a string or boolean 
// age = 'a'  // this will give us an error because we have set age to a number and now we are trying to set it to a string
